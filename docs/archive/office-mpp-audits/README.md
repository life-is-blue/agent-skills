# Archived office-mpp audits

这些材料是 2026 年 4 月形成的阶段性审计记录，仅用于追溯当时的分析过程。部分问题
已经修复，部分结论未经可靠测试验证，还有部分命令描述已与当前实现不一致。

不要将这里的内容作为当前接口或行为依据。当前真相的优先级是：

1. `skills/office-mpp/scripts/` 中的实现与本地 `--help` 输出；
2. 可重复运行的测试；
3. `skills/office-mpp/SKILL.md` 与 `references/`；
4. 本目录中的历史审计材料。

若归档材料提出的问题仍然有效，应以失败测试或新的 `docs/plans/` 任务重新进入维护
流程，不要直接恢复旧审计中的修复建议。
